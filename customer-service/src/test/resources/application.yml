spring:
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  zipkin:
    enabled: false

eureka:
  client:
    enabled: false